<?xml version="1.0" encoding="UTF-8"?>
<project name="Development" description="Deploy the components and modules to server" default="deploy">
	<property name="dirs.root" value=".."/>
	<property file="./deploy.properties"/>

	<taskdef name="zipme" classname="phingext.ZipmeTask" />

	<target name="setup-properties"
	        description="Set up version and build properties">
		<tstamp>
			<format property="build.date"
			        pattern="%Y-%m-%d"/>
		</tstamp>
	</target>

	<target name="deploy" description="Deploy all source code of module and component">
		<if>
			<equals arg1="${web.root}" arg2="" casesensitive="false" />
			<then>
				<echo>Do not input web root to deploy</echo>
			</then>
			<else>
				<phingcall target="execute" />
			</else>
		</if>
	</target>

	<target name="execute">
		<echo>${host.root}/${web.root}/</echo>
	</target>
</project>